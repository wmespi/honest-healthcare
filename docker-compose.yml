services:
  db:
    image: postgis/postgis:15-3.3-alpine
    container_name: honest-healthcare-db
    command: >
      postgres
      -c max_wal_size=4GB
      -c checkpoint_timeout=15min
      -c log_min_messages=warning
      -c log_checkpoints=off
    environment:
      - POSTGRES_DB=honest_healthcare
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./data-test:/data-test
      - ./data:/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5

  etl:
    build:
      context: .
      dockerfile: deploy/Dockerfile.etl
    container_name: honest-healthcare-etl
    init: true
    command: tail -f /dev/null
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=postgres://postgres:postgres@db:5432/honest_healthcare?sslmode=disable
      - TEST_DATABASE_URL=postgres://postgres:postgres@db:5432/honest_healthcare?sslmode=disable&search_path=test
    depends_on:
      db:
        condition: service_healthy

  serving:
    build:
      context: .
      dockerfile: deploy/Dockerfile.serving
    container_name: honest-healthcare-serving
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DATA_DIR=/app/data

  frontend:
    build:
      context: .
      dockerfile: deploy/Dockerfile.frontend
      target: dev
    container_name: honest-healthcare-frontend
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      # Override via .env (LAN_HOST=10.0.0.76) to reach the app from another
      # device on the same network — e.g. a phone at http://<LAN_HOST>:5173.
      - VITE_API_URL=http://${LAN_HOST:-localhost}:8000
    depends_on:
      - serving

volumes:
  postgres_data:
