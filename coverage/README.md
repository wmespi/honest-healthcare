# Coverage snapshots

Generated by `scripts/coverage_probe.py` (API scorecard) and
`scripts/coverage_report.py` (Postgres `coverage_log` aggregate). Regenerate with:

```
make cov-probe LABEL=<label>     # writes data/anthem/coverage_scorecard.json
make cov-report                  # prints the coverage_log aggregate
```

| file | what |
|---|---|
| `scorecard_before.md` | 40-code basket, **8 baseline parquet files** (19.3M rates) |
| `scorecard_after.md` / `.json` | same basket, **after the GA/individual parse batch** (20.5M rates) |
| `coverage_log_report.md` | per-file contribution of the 25-file batch |

## Headline

The batch's decisive file is `anthem/GA_JBNKMED0001.json.gz` (1.1 MB) — the individual
Georgia plan file — contributing **684,959 rate rows / 85,687 provider rows / 15,560 billing
codes / 36,946 NPIs**, all attributed to `network_name = "GA Blue Value HIX Individual
Network"` (the target plan's network). Before the batch that network had 0 attributed rates;
after, 684,959.

Per-code provider-group counts for the target basket rose across the board
(99213: 512 → 650, 99214: 202 → 330, labs roughly doubled, behavioral health +40%).

Remaining gap: `00810` (anesthesia) — no CPT fee-schedule rates in any file parsed so far;
anesthesia is typically priced in base units, not a flat CPT amount.
